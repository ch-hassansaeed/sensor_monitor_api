<form method="post" action="{{ path('sensor_detail_delete', {'id': sensor_detail.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sensor_detail.id) }}">
    <button class="btn">Delete</button>
</form>
